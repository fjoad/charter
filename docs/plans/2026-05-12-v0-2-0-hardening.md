# v0.2.0 Hardening Plan

**Status:** ✅ Complete (2026-05-12) — all 6 items shipped, 32/32 tests pass, merged to main.
**Branch:** `feat/v0.2.0-hardening` (dogfoods branch-with-dots edge case)

**Goal:** Close the polish gaps identified after v0.2.0 merge before push.

## Scope

Six items, all addressed in one branch, no version bump (folded into v0.2.0):

1. **Edge case tests + slugify guard** — branches with dots (`feat/v0.2.0-hardening`), capitals, very short tails (< 3 chars). Add min-length guard to avoid false-positive slug matches.
2. **turn-nudge.sh tests** — no scaffold → silent; with scaffold → emits nudge JSON.
3. **JSON validity tests** — plugin.json, package.json, hooks.json parse correctly; hooks.json command paths reference real files.
4. **Command content tests** — charter-finish.md has both "Main Branch Flow" and "Feature Branch Flow"; charter-adopt.md has the `branches` convention block.
5. **package.json `scripts.test` entry** — `npm test` invokes `verify-plugin.sh`.
6. **GitHub Actions CI** — `.github/workflows/test.yml` runs `verify-plugin.sh` on push and PR.

Also: bump ARCHITECTURE.md "Last updated" date (was 2026-04-17, content has changed).

## Non-Goals

- New user-visible behavior (this is polish only).
- Version bump (rolled into v0.2.0 since not pushed yet).
- Testing model behavior of markdown commands (inherent limit).
